<%= form_with(model: card, local: true) do |form| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :opal_number %>
    <%= form.number_field :opal_number %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.hidden_field :profile_id, :value => @profile_id  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
