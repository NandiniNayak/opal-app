<h1>CODER ATTENDANCE </h1>

<%# if educator you must be able to see all the users %>. 
<%# link to register the card %>


<%# |user name (link) ----- Register Card| %>

<p id="notice"><%= notice %></p>

<h1>Enrollments</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Role</th>
      <th>Course</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profiles.each do |profile| %>
      <tr>
        <td><%= link_to profile.user["name"], attendances_path %></td>
        <td><%= profile.role %></td>
        <td><%= link_to 'Register Card Number', new_card_path %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Profile', new_profile_path %>

